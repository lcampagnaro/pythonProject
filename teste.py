from Ex107 import moeda

p = float(input('Digite o preço: R$'))
taxaa = float(input('Digite a taxa de aumento necessário: '))
taxar = float(input('Digite a taxa de desconto para cálcular: '))
moeda.resumo(p, taxaa, taxar)

